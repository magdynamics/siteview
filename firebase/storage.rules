rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /photos/{siteId}/{userId}/{photoId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    match /documents/{siteId}/{userId}/{docId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
